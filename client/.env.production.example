# Production build config — copy to .env.production before `npm run build`
# Point these at your deployed backend domain (HTTPS required).
VITE_API_BASE_URL=https://api.yourdomain.com/api
VITE_API_SOCKET_URL=https://api.yourdomain.com
